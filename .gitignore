# Bí mật + dữ liệu runtime - KHÔNG commit.
.license
.update-state
proxy/.env
bin/wp-cli.phar

# Payload (plugin/theme) tải từ app.lat.vn gated license - không track.
payload/plugins/
payload/themes/
payload/mu-plugins/
